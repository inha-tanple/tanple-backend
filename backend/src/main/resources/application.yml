spring:
  profiles:
    active: datasource, cloud
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

springdoc:
  swagger-ui:
    path: /docs
  show-login-endpoint: true

logging:
  level:
    root: INFO
    org:
      springframework:
        web: DEBUG
